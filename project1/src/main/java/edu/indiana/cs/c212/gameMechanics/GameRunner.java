package edu.indiana.cs.c212.gameMechanics;

import java.util.List;
import java.util.ArrayList;
import edu.indiana.cs.c212.board.Tile;
import edu.indiana.cs.c212.players.*;
import java.util.Scanner;

public class GameRunner{
	public static void main(String[] args) {
        // use commandlineview
        Scanner scanner = new Scanner(System.in);
        CommandLineView.setup(scanner);
	}
	
    public Player boolean run(Move m)
    if(players.getMove()).equals




public OverwriteMove(playerColor color, Tile t){ 		
	if(color.equalsplayerColor.BLANK){
		return null;
	}
	else if(color.equalsplayerColor.RED){
		t.setColor(BLUE);
	}
	else if(color.equalsplayerColor.BLUE){
		t.setColor(RED);
	}
}
    
public OverwriteRules(Move z){
	if(z == !overwriteMove(
		re
	}
}
    
    
    
}