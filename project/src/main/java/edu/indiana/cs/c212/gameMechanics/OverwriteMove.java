package edu.indiana.cs.c212.gameMechanics;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.Set;

import edu.indiana.cs.c212.board.Tile;
import edu.indiana.cs.c212.exceptions.*;

public class OverwriteMove {

 public OverwriteMove(PlayerColor color, Tile t){ 
     if(!color.equals(PlayerColor.BLANK)){
     }
     if(color.equals(PlayerColor.RED)){
			t.setColor(PlayerColor.BLUE);
		}
		else if(color.equals(PlayerColor.BLUE)){
			t.setColor(PlayerColor.RED);
		}
	}


public OverwriteRules(Move m, Tile t) throws InvalidMoveException{
	if(!m.OverwriteMove(PlayerColor color, Tile t).equals(PlayerColor.RED || PlayerColor.BLUE){
		int tX = start1.x;
		int tY = start1.y;
		Set<Tile> blank = new HashSet<Tile>();
		Set<Tile> red = new HashSet<Tile>();
		Set<Tile> blue = new HashSet<Tile>();
		for(x; x = -1; x++)
			if (board.getTileAt(tX - 1, tY).getColor() == color){
			for(y; y = -1; y++)
				
			
			
 
	}
	}
}


}